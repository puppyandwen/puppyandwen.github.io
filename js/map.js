const batches = [
  {
    id: "osaka-2025",
    lat: 37.6937,
    lon: 135.5023,
    offsetX: 120,
    offsetY: -80,
    scale: 1.4,
    time: "2025.04.26 - 05.02",
    photos: [
      { src: "../images/year3/photo69.jpg", caption: "4月26日 坐上Haruka列车啦~Hello Kitty很可爱呀" },
      { src: "../images/year3/photo70.jpg", caption: "4月26日 日本的第一口抹茶！就爱这口齁甜！" },
      { src: "../images/year3/photo71.jpg", caption: "4月26日 史努比松饼！我超级喜欢史努比！" },
      { src: "../images/year3/photo72.jpg", caption: "4月27日 老京都市区一日游哦~这一天去了清水寺和鸭川！" },
      { src: "../images/year3/photo73.jpg", caption: "4月28日 坐小火车去看伏见稻荷大社~感觉太阳出来之前阴森森的" },
      { src: "../images/year3/photo74.jpg", caption: "4月29日 来到大阪，确实是完全不一样的风味呢，很难用国内任何一座其他城市比拟" },
      { src: "../images/year3/photo75.jpg", caption: "4月29日 我们在大阪住的地方，H2H心斋桥，是在一起以来住过的top理想的环境之一了" },
      { src: "../images/year3/photo76.jpg", caption: "4月30日 在心斋桥开启新的一天！各种商场都有很多很漂亮的打卡点" },
      { src: "../images/year3/photo77.jpg", caption: "4月30日 在商场里和史努比的合影~" },
      { src: "../images/year3/photo78.jpg", caption: "4月30日 标志性游客照1 雯雯雾蓝色的帽子太适合日本这段时间的气候了" },
      { src: "../images/year3/photo79.jpg", caption: "4月30日 天王寺，紧赶慢赶在日落前找到一个打卡点" },
      { src: "../images/year3/photo80.jpg", caption: "4月30日 雯雯力竭之前最后陪姜姜逛了一整栋楼的玩具和模型，姜姜感动" },
      { src: "../images/year3/photo81.jpg", caption: "5月1日 新的一天，依靠蛋酱三明治补充新的能量！在奈良的草地上尽情奔跑释放吧" },
      { src: "../images/year3/photo82.jpg", caption: "5月2日 乘坐haruka返回大阪关西机场啦，依依不舍中。。。和雯雯的第一次出国，美好的回忆！" }
    ]
  },
  {
    id: "shanghai 2025",
    lat: 34,
    lon: 121,
    offsetX: 80,
    offsetY: 40,
    scale: 1.4,
    time: "2025.5.3 - 6.30",
    photos: [
      { src: "../images/year3/photo83.jpg", caption: "5月9日 从日本回来之后雯雯的常态，太喜欢雯雯无忧无虑睡觉时候的样子了" },
      { src: "../images/year3/photo84.jpg", caption: "5月20日 和雯雯解锁了新娱乐：台球！" },
      { src: "../images/year3/photo85.jpg", caption: "5月29日 没有压力的雯雯就是无敌的！就算天天和姜姜嘎嘎吃，依旧身材干柴" },
      { src: "../images/year3/photo86.jpg", caption: "6月9日 睡不醒的雯，吃不下的蛋糕和不留遗憾的欧国联！在一起之后看得第二精彩的一场球" },
      { src: "../images/year3/photo87.jpg", caption: "6月28日 夏天到了，和雯雯一起吃了一整个榴莲！" },
      { src: "../images/year3/photo88.jpg", caption: "6月30日 出发前的一天，雯雯和姜姜还是满心期待又有点隐隐害怕" },
    ]
  },
  {
    id: "wellington 2025",
    lat: -43,
    lon: 175,
    offsetX: -100,
    offsetY: -180,
    scale: 1.4,
    time: "2025.7.1 - 7.10",
    photos: [
      { src: "../images/year3/photo89.jpg", caption: "7月1日 深夜到达惠灵顿，姜对这里的初印象就是这里安安静静的" },
      { src: "../images/year3/photo90.jpg", caption: "7月2日 休息一天~起床的雯雯开始做咖啡" },
      { src: "../images/year3/photo91.jpg", caption: "7月2日 第一顿正餐，姜念念不忘的hell pizza" },
      { src: "../images/year3/photo92.jpg", caption: "7月3日 去上学啦" },
      { src: "../images/year3/photo93.jpg", caption: "7月3日 晚上参加了开学迎新会，一起蹭饭的一天" },
      { src: "../images/year3/photo94.jpg", caption: "7月4日 接收雯雯宿舍！当时还只是一个空空荡荡的物资，现在已经满满雯雯气息啦" },
      { src: "../images/year3/photo95.jpg", caption: "7月5日 新西兰爱心厨房第一弹！煎牛排配贝果！" },
      { src: "../images/year3/photo96.jpg", caption: "7月6日 姜姜自己出去玩儿，在山顶喝了白咖啡" },
      { src: "../images/year3/photo97.jpg", caption: "7月8日 爱心厨房第二弹！超级健康的泡面汤配虾虾" },
      { src: "../images/year3/photo97.jpg", caption: "7月9日 我的愿望是，发现意义或是发现不需要意义，雯雯都要好好地生活！" },
      { src: "../images/year3/photo98.jpg", caption: "7月9日 姜姜自己出去玩儿，在山顶喝了白咖啡" },
      { src: "../images/year3/photo99.jpg", caption: "7月9日 雯雯地绒绒帽帽好暖暖，头贴在上面就不想分开了" },
      { src: "../images/year3/photo100.jpg", caption: "7月10日 乡愁是一片小小的安检区" }
    ]
  },
  {
    id: "shanghai 2025",
    lat: 32,
    lon: 121,
    offsetX: -100,
    offsetY: 160,
    scale: 1.4,
    time: "2025.11.21 - 12.7",
    photos: [
      { src: "../images/year3/photo101.jpg", caption: "11月21日 雯雯如闪电般归来！" },
      { src: "../images/year3/photo102.jpg", caption: "11月24日 休息了一个周末，雯雯开始回归图书馆" },
      { src: "../images/year3/photo103.jpg", caption: "11月26日 下班之后，一起去看疯狂动物城首映吧" },
      { src: "../images/year3/photo104.jpg", caption: "11月30日 吼吼，啥好事都给雯雯碰着了！一起看到了齐达内大活人！" },
      { src: "../images/year3/photo105.jpg", caption: "12月5日 带雯雯爸爸妈妈一起逛了交大" },
      { src: "../images/year3/photo106.jpg", caption: "12月7日 要怎么收藏，要怎么拥有" },
    ]
  },
  {
    id: "southisland 2026",
    lat: -47,
    lon: 171,
    offsetX: -250,
    offsetY: -20,
    scale: 1.4,
    time: "2026.2.3 - 2.14",
    photos: [
      { src: "../images/year3/photo107.jpg", caption: "2月2日 姜姜踏上旅途~" },
      { src: "../images/year3/photo108.jpg", caption: "2月3日 到达之后陪伴雯雯吃的第一顿meal，真的好好吃呀韩国炸鸡" },
      { src: "../images/year3/photo109.jpg", caption: "2月4日 基督城！姜在电话里陪伴雯雯走过的riverside market，现在亲自到达啦~" },
      { src: "../images/year3/photo110.jpg", caption: "2月5日 自驾出发！在Ashburton中途休息尝试了蓝纹奶酪~感觉还不错呀" },
      { src: "../images/year3/photo111.jpg", caption: "2月5日 蒂卡波湖边太阳好晒哦，下午拍了一个小时不到的照片就回家休息惹" },
      { src: "../images/year3/photo112.jpg", caption: "2月5日 住在Fairlie~Fairlie有两大宝，第一是农场里面有羊驼" },
      { src: "../images/year3/photo113.jpg", caption: "2月6日 Fiarlie第二大宝，bakehouse的派~一口气吃掉四个！！"},
      { src: "../images/year3/photo114.jpg", caption: "2月6日 普卡基湖边吃三文鱼！姜姜最爱吃的就是三文鱼了嘻嘻"},
      { src: "../images/year3/photo115.jpg", caption: "2月6日 特威泽尔镇上超市随便买买也能组成大餐~雯雯最爱的奶酪品牌从此发现~"},
      { src: "../images/year3/photo116.jpg", caption: "2月7日 Wanaka湖旁边的Luggate小镇~姜姜第一次亲手摸金毛狗狗！是一只淡狗"},
      { src: "../images/year3/photo117.jpg", caption: "2月8日 在湖边的丰盛一餐~餐车上买到的贝果"},
      { src: "../images/year3/photo118.jpg", caption: "2月8日 狙击手雯雯"},
      { src: "../images/year3/photo119.jpg", caption: "2月8日 爱心厨房之"},
      { src: "../images/year3/photo120.jpg", caption: "2月9日 你好邦尼~欢迎加入兔兔大家族"},
      { src: "../images/year3/photo121.jpg", caption: "2月10日 皇后镇的狗狗是一只浓狗"},
      { src: "../images/year3/photo122.jpg", caption: "2月10日 爱心厨房之~"},
      { src: "../images/year3/photo123.jpg", caption: "2月11日 再见啦车车~和雯雯第一次成功境外自驾游！姜姜自豪~"},
      { src: "../images/year3/photo124.jpg", caption: "2月12日 回到惠灵顿，继续陪雯雯一起吃吃吃！"},
      { src: "../images/year3/photo125.jpg", caption: "2月13日 爱心厨房之爱心甜点~姜姜都吃完了哦，下次还要和雯雯一起做好吃的！"},
      { src: "../images/year3/photo126.jpg", caption: "2月14日 舍不得兔兔！要早点度过时间再见面呀~姜姜会乖乖等待的！"},
    ]
  },
];



// 经纬度-坐标
// function project(lat, lon) {
//   const map = document.getElementById("map");

//   const width = map.offsetWidth;
//   const height = map.offsetHeight;

//   const x = (lon + 180) / 360 * width;
//   const y = (90 - lat) / 180 * height;

//   return { x, y };
// }

function project(lat, lon) {
  const map = document.getElementById("map");
  const rect = map.getBoundingClientRect();

  const x = (lon + 180) / 720 * rect.width;
  const y = (90 - lat) / 180 * rect.height;

  return {
    x: rect.left + x,
    y: rect.top + y
  };
}

// 经度中心
const CENTER_LON = 145;
function updateMapPosition() {
  const map = document.getElementById("map");
  const rect = map.getBoundingClientRect();

  const mapWidth = rect.width;

  // 经度 → x
  const centerX = (CENTER_LON +180 ) / 720 * mapWidth;

  // 屏幕中心
  const screenCenter = window.innerWidth / 2;

  // 偏移
  const offset = screenCenter - centerX;

  map.style.transform = `translateX(${offset}px)`;
}
updateMapPosition()
window.addEventListener("resize", updateMapPosition);
window.addEventListener("load", updateMapPosition);



//渲染batch
function batch_render_backup(){
  const container = document.getElementById("batches");

  batches.forEach(batch => {
    const { x, y } = project(batch.lat, batch.lon);

    const el = document.createElement("div");
    el.className = "batch";
    el.style.left = x + "px";
    el.style.top = y + "px";

    el.innerHTML = `
      <div class="bubble">
        <div class="thumb-row">
          ${batch.photos.slice(0,5).map(p => `<img src="${p.src}">`).join("")}
        </div>
        <div class="time-label">${batch.time}</div>
      </div>
    `;

    el.onclick = () => openViewer(batch);

    container.appendChild(el);
  });

}

function batch_render(){
  const container = document.getElementById("batches");
  container.innerHTML = "";

  batches.forEach(batch => {

    const { x, y } = project(batch.lat, batch.lon);

    const el = document.createElement("div");
    el.className = "batch";

    // ✅ pin位置（真实地理位置）
    el.style.left = x + "px";
    el.style.top = y + "px";

    el.innerHTML = `
      <div class="pin"></div>

      <div class="line"></div>

      <div class="bubble-wrap"
          style="transform: translate(${batch.offsetX}px, ${batch.offsetY}px) scale(${batch.scale || 1.4}">
        <div class="bubble">
          <div class="thumb-row">
            ${batch.photos.slice(0,5).map(p => `<img src="${p.src}">`).join("")}
          </div>
          <div class="time-label">${batch.time}</div>
        </div>
      </div>
    `;




    

    container.appendChild(el);

    function updateLine(el, batch){
      const line = el.querySelector(".line");
    
      const dx = batch.offsetX;
      const dy = batch.offsetY;
    
      const length = Math.sqrt(dx*dx + dy*dy);
      const angle = Math.atan2(dy, dx) * 180 / Math.PI;
    
      line.style.height = length + "px";
      line.style.transform = `rotate(${angle-90}deg)`;
    }

    updateLine(el, batch)



    const wrap = el.querySelector(".bubble-wrap");


    let dragging = false;
    let startX, startY;
    let originX = batch.offsetX;
    let originY = batch.offsetY;
    let moved = false;

    wrap.addEventListener("pointerdown", (e) => {
      e.stopPropagation();

      dragging = true;
      moved = false;
      startX = e.clientX;
      startY = e.clientY;

      wrap.setPointerCapture(e.pointerId);
    });

    window.addEventListener("pointermove", (e) => {
      if (!dragging) return;

      moved = true;

      const dx = e.clientX - startX;
      const dy = e.clientY - startY;

      batch.offsetX = originX + dx;
      batch.offsetY = originY + dy;

      wrap.style.transform =
        `translate(${batch.offsetX}px, ${batch.offsetY}px) scale(${batch.scale || 1.4})`;

      updateLine(el, batch); // 👈 让线跟着动
    });

    window.addEventListener("pointerup", (e) => {
      if (dragging) {
        dragging = false;

        // 更新初始值（下一次拖拽用）
        originX = batch.offsetX;
        originY = batch.offsetY;

        wrap.releasePointerCapture(e.pointerId);
      }
    });


    // wrap.addEventListener("touchstart", (e) => {
    //   e.stopPropagation();

    //   dragging = true;
    //   moved = false;
    //   startX = e.clientX;
    //   startY = e.clientY;
    // });

    // window.addEventListener("touchmove", (e) => {
    //   if (!dragging) return;

    //   moved = true;

    //   const dx = e.clientX - startX;
    //   const dy = e.clientY - startY;

    //   batch.offsetX = originX + dx;
    //   batch.offsetY = originY + dy;

    //   wrap.style.transform =
    //     `translate(${batch.offsetX}px, ${batch.offsetY}px) scale(${batch.scale || 1.4})`;

    //   updateLine(el, batch); // 👈 让线跟着动
    // });

    // window.addEventListener("touchend", () => {
    //   if (dragging) {
    //     dragging = false;

    //     // 更新初始值（下一次拖拽用）
    //     originX = batch.offsetX;
    //     originY = batch.offsetY;
    //   }
    // });


    el.onclick = () => {
      if (!moved) openViewer(batch);
    };
    // el.onclick = () => openViewer(batch);
  });
}



//Viewer 进入batch
let currentIndex = 0
const viewer = document.getElementById("viewer");
const track = document.querySelector(".viewer-track");

function openViewer(batch) {
  viewer.classList.remove("hidden");
  track.innerHTML = "";

  currentIndex = 0;

  batch.photos.forEach(p => {
    const item = document.createElement("div");
    item.className = "viewer-item";

    item.innerHTML = `
      <img src="${p.src}">
      <div class="caption">${p.caption}</div>
    `;

    track.appendChild(item);
  });
  updateViewer();
}

function updateViewer(){
  track.style.transform = `translateX(-${currentIndex * 100}%)`;
}

viewer.addEventListener("click", (e) => {
  if (e.target === viewer) {
    viewer.classList.add("hidden");
    return;
  }
  if (e.target.classList.contains("left")) {
    e.stopPropagation();
    prev();
    return;
  }
  if (e.target.classList.contains("right")) {
    e.stopPropagation();
    next();
    return;
  }

  if (e.target === viewer) {
    viewer.classList.add("hidden");
  }

  // 👉 左右点击区域判断
  const rect = viewer.getBoundingClientRect();
  const x = e.clientX - rect.left;

  if (x > rect.width / 2) {
    next();
  } else {
    prev();
  }


});

window.addEventListener("keydown", (e) => {
  if (viewer.classList.contains("hidden")) return;

  if (e.key === "ArrowRight") next();
  if (e.key === "ArrowLeft") prev();
  if (e.key === "Escape") viewer.classList.add("hidden");
});

function next() {
  const total = track.children.length;
  if (currentIndex < total - 1) {
    currentIndex++;
    updateViewer();
  }
}

function prev() {
  if (currentIndex > 0) {
    currentIndex--;
    updateViewer();
  }
}

// viewer.onclick = (e) => {
//   if (e.target === viewer) {
//     viewer.classList.add("hidden");
//   }
// };

//晨昏线
const canvas = document.getElementById("terminator");
const ctx = canvas.getContext("2d");

function drawTerminator() {
  const canvas = document.getElementById("terminator");
  const ctx = canvas.getContext("2d");

  canvas.width = window.innerWidth;
  canvas.height = window.innerHeight;

  ctx.clearRect(0,0,canvas.width,canvas.height);

  const map = document.getElementById("map");
  const mapRect = map.getBoundingClientRect();
  const canvasRect = canvas.getBoundingClientRect();

  //计算地图在canvas坐标系中的偏移量
  const offsetX = mapRect.left - canvasRect.left;
  const offsetY = mapRect.top - canvasRect.top;

  const now = new Date();
  //儒略日计算
  const day = Math.floor(
    (now - new Date(now.getFullYear(),0,0)) / 86400000
  );

  const declDeg = 23.44 * Math.sin((2*Math.PI/365)*(day-81));
  const declRad = declDeg * Math.PI / 180;

  const hours = now.getUTCHours() + now.getUTCMinutes()/60;

  let subsolarLon = - (hours - 12) * 15;
  subsolarLon = ((subsolarLon % 360) + 360) % 360;
  if (subsolarLon>180) subsolarLon -= 360;

  ctx.fillStyle = "rgba(0,0,0,0.25)";

  ctx.beginPath();

  let first = true;

  for (let x=0; x<canvas.width; x++){

    let lon = (x - mapRect.left) / mapRect.width * 720 - 180
    let hourAngleDeg = lon - subsolarLon;
    hourAngleDeg = ((hourAngleDeg % 360) + 360) % 360;
    if (hourAngleDeg > 180) hourAngleDeg -= 360;
    const hourAngleRad = hourAngleDeg * Math.PI / 180;

    const cosH = Math.cos(hourAngleRad);
    const tanDelta = Math.tan(declRad);

    let latRad;
    // 处理 δ=0 且 cosH=0 的罕见情况 (整条经线为晨昏线)
    if (Math.abs(declRad) < 1e-8 && Math.abs(cosH) < 1e-8) {
      latRad = 0; // 取赤道作为代表点
    } else {
      // 通用公式: φ = atan2(-cosH, tanδ)
      latRad = Math.atan2(-cosH, tanDelta);
    }

    let latDeg = latRad * 180 / Math.PI;
    // 确保纬度范围在 [-90, 90] 内
    latDeg = Math.min(90, Math.max(-90, latDeg));

    y = (90 - latDeg) / 180 * mapRect.height + mapRect.top
    if(first){
      ctx.moveTo(x,y);
      first = false;
    }else{
      ctx.lineTo(x,y);
    }
  }





  

  // for(let y=0;y<=canvas.height;y++){

  //   const lat = 90 - (y / canvas.height) * 180;

  //   const latRad = lat * Math.PI/180;
  //   const declRad = decl * Math.PI/180;

  //   const lon = subsolarLon +
  //     Math.atan(-Math.cos(latRad)/Math.tan(declRad)) * 180/Math.PI;

  //   const x = rect.left + ((lon + 180)/720)*rect.width;
  //   console.log(lat, lon)

  //   if(first){
  //     ctx.moveTo(x,y);
  //     first = false;
  //   }else{
  //     ctx.lineTo(x,y);
  //   }
  // }

  // ✅ 关键：只封闭“夜侧”
  if (day >= 81 && day <= 266){
    ctx.lineTo(canvas.width, canvas.height);
    ctx.lineTo(0, canvas.height);
  }else{
    ctx.lineTo(canvas.width, 0);
    ctx.lineTo(0, 0);
  }
  

  ctx.closePath();
  ctx.fill();
}



function debugPoint(lat, lon) {
  const { x, y } = project(lat, lon);

  const dot = document.createElement("div");
  dot.style.position = "absolute";
  dot.style.left = x + "px";
  dot.style.top = y + "px";
  dot.style.width = "6px";
  dot.style.height = "6px";
  dot.style.background = "red";
  dot.style.borderRadius = "50%";

  document.body.appendChild(dot);
}

// debugPoint(36.69, 135.50);
batch_render()
setInterval(drawTerminator, 60000);
drawTerminator();